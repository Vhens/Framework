import React, { PureComponent, PropTypes } from 'react';

export default class Footer extends PureComponent {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <div>
        footer
      </div>
    );
  }
}
